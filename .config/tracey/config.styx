// Tracey configuration for trame

specs (
    {
        name trame
        prefix t
        include (docs/content/**/*.md)
        impls (
            {
                name rust
                include (trame/**/*.rs)
                exclude (target/**)
            }
        )
    }
)
