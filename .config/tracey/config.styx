// Tracey configuration for trame

specs (
    {
        name trame
        include (docs/content/**/*.md)
        impls (
            {
                name rust
                include (trame/**/*.rs)
                exclude (target/**)
            }
        )
    }
)
