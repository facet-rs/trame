list:
    just --list

all:
    just kani
    just test

# Run proofs via soteria-rust. Use `--filter substring` to run only some tests
prove *args:
    soteria-rust cargo . --kani --summary --stats stdout --flamegraph-dir .soteria {{ args }}

test *args:
    cargo test -p trame-formal {{ args }}
